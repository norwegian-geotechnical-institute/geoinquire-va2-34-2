# Hazard tool runtime configuration
# mode: "constant" or "map"
mode: map

# Used when mode is "constant"
input_rain: 80 # (24h accumulated rainfall in mm)

# Used when mode is "map" (set the .tif filename without extension)
#input_rain_map_name: your_rainfall_map_name
input_rain_map_name: Rain24h/SeNorgeTif/20230808_rr_seNorge2018
